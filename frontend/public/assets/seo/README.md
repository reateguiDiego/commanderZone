# Search Console verification

Prefer DNS TXT verification.

If Search Console HTML file verification is required, place the exact Google-provided `google*.html` file in `frontend/public/` so it is deployed from the public root.

Do not commit placeholder, fake, personal, or guessed verification tokens.
