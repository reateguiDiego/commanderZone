name: PR Required Checks

on:
  pull_request:
    branches:
      - main

concurrency:
  group: pr-required-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: backend
    uses: ./.github/workflows/backend-ci-reusable.yml

  frontend:
    name: frontend
    uses: ./.github/workflows/frontend-ci-reusable.yml

  go-runtime:
    name: go-runtime
    uses: ./.github/workflows/go-runtime-ci-reusable.yml

  required:
    name: required
    runs-on: ubuntu-latest
    needs:
      - backend
      - frontend
      - go-runtime
    if: always()
    steps:
      - name: Verify required checks succeeded
        shell: bash
        run: |
          set -euo pipefail
          BACKEND_RESULT="${{ needs.backend.result }}"
          FRONTEND_RESULT="${{ needs.frontend.result }}"
          GO_RUNTIME_RESULT="${{ needs.go-runtime.result }}"

          echo "backend: $BACKEND_RESULT"
          echo "frontend: $FRONTEND_RESULT"
          echo "go-runtime: $GO_RUNTIME_RESULT"

          if [ "$BACKEND_RESULT" != "success" ] || [ "$FRONTEND_RESULT" != "success" ] || [ "$GO_RUNTIME_RESULT" != "success" ]; then
            echo "At least one required check failed." >&2
            exit 1
          fi
